# `README.md`

```markdown
# 🎧 LyricEar

**Тренажёр восприятия иностранных текстов на слух.**

Слушаешь песню фрагмент за фрагментом, записываешь то, что услышал, сверяешь с оригиналом — и постепенно начинаешь разбирать каждое слово. Весь прогресс хранится локально в браузере, ничего не отправляется на сервер.

---

## 🚀 Быстрый старт

1. Откройте `index.html` — каталог песен.
2. Выберите песню — откроется плеер (`player.html`).
3. Нажмите **📁 Выбрать файл** и укажите аудио- или видеофайл песни.
   - Файл сохраняется в IndexedDB браузера — при следующем визите загружать повторно не нужно.
   - Если автор предоставил ссылку на Яндекс.Диск — кнопка **☁ Яндекс.Диск** откроет её.
4. Расставьте метки **Start / End** для каждой строки (или они уже проставлены в JSON).
5. Слушайте фрагмент, пишите в поле «Как услышал(а)…», нажимайте **👁 Показать** для сверки.
6. Когда строка освоена — отмечайте **Выучено ✓**.

---

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---|---|
| `Space` | Play / Pause |
| `S` | Поставить метку **Start** на текущей позиции |
| `E` | Поставить метку **End** и перейти к следующей строке |
| `R` | Воспроизвести текущий фрагмент (Start → End) |
| `↑` / `P` | Предыдущая строка |
| `↓` / `N` | Следующая строка |

> Клавиши работают, когда фокус **не** в текстовом поле.

---

## 🗂 Структура проекта

```
lyricear/
├── index.html              # Каталог песен (домашняя страница)
├── player.html             # Универсальный плеер для любой песни
├── assets/
│   ├── styles.css          # Все стили (тёмная тема)
│   └── app.js              # Вся логика: плеер, спектрограмма, прогресс
├── data/
│   ├── catalog.json        # Список песен для каталога
│   └── songs/
│       └── store-hav.json  # JSON с текстом, фонетикой, переводами
└── songs/
    └── store-hav/
        └── index.html      # Редирект → player.html?song=store-hav
```

### Ключевые файлы

| Файл | Назначение |
|---|---|
| `player.html` | Единая страница плеера. Получает параметр `?song=<slug>`, загружает `data/songs/<slug>.json` |
| `app.js` | Загрузка JSON, управление `<video>`, спектрограмма (Web Audio API), сохранение прогресса в `localStorage` и медиа в `IndexedDB` |
| `styles.css` | Тёмная тема, sticky-плеер, адаптив, видео/аудио режимы |
| `catalog.json` | Массив `{ songs: [...], languages: [...] }` для главной страницы |
| `<slug>.json` | Данные одной песни: метаданные, строки с текстом, фонетикой, переводами, таймингами |

---

## 🎵 Формат JSON песни

```jsonc
{
  "song": {
    "id": "store-hav",
    "title": "Stor og hav",
    "artist": "Исполнитель",
    "language": "no",
    "languageName": "Норвежский",
    "hint": "Подсказка для ученика",
    "media": {
      "yadisk": "https://disk.yandex.ru/d/..."   // необязательно
    }
  },
  "ui": {
    "showOriginalByDefault": false,
    "showTranslationByDefault": false,
    "showPhoneticByDefault": false,
    "showWhyHeardByDefault": false
  },
  "items": [
    {
      "id": "store-hav-001",
      "text": "Оригинальный текст строки",
      "phonetic": "фонетическая подсказка автора",
      "translation": "Перевод на русский",
      "why": "Почему слышится именно так",
      "confidence": 0.85,
      "start": 12.40,
      "end": 16.20,
      "learned": false,
      "phonetic_user": ""
    }
  ]
}
```

---

## 🔧 Возможности

- **🎬 Видео и аудио** — автоматическое определение типа файла; видео показывается в `16:9`, аудио — только плеер
- **📊 Спектрограмма** — реалтайм-визуализация через Web Audio API с масштабированием (×1 – ×4)
- **📌 Sticky-плеер** — панель управления прилипает к верху при прокрутке строк
- **🔁 Зацикливание** — фрагмент повторяется до отключения
- **⏭ Авто-переход** — после окончания фрагмента автоматически переходит к следующей строке
- **💾 Локальное хранение** — прогресс в `localStorage`, медиафайл в `IndexedDB`
- **🗑 Управление кэшем** — кнопка «Забыть» удаляет медиа из браузера
- **📱 Адаптивный дизайн** — работает на телефоне и десктопе

---

## 📋 Как добавить новую песню

1. Создайте `data/songs/<slug>.json` по формату выше.
2. Добавьте запись в `data/catalog.json` → массив `songs`:
   ```json
   {
     "id": "my-song",
     "title": "Название",
     "artist": "Исполнитель",
     "language": "en",
     "languageName": "Английский",
     "short": "Краткое описание",
     "url": "player.html?song=my-song"
   }
   ```
3. (Опционально) создайте `songs/my-song/index.html` с редиректом:
   ```html
   <!doctype html><meta http-equiv="refresh" content="0;url=../../player.html?song=my-song">
   ```

---

## 🛡 Приватность

- Медиафайлы **не загружаются на сервер** — всё хранится в `IndexedDB` вашего браузера.
- Прогресс сохраняется в `localStorage` — только на вашем устройстве.
- Сайт работает полностью статически — можно открывать через `file://` или любой статический хостинг.

---

## 📄 Лицензия

MIT
```

---

Готово. Скопируй в `README.md` в корень репозитория, закоммить и запуши.
